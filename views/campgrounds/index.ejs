<!-- <%- include('partials/header.ejs', { title: "Home page" }) %>

    <body>
        <%- include('partials/navbar.ejs') %>
            <div class="max-w-2xl mx-auto mt-10 p-6 bg-white shadow-lg rounded-lg">
                <h2 class="text-2xl font-bold text-gray-700 mb-4">Campgrounds</h2>
                <ul class="space-y-2">
                    <% campgrounds.forEach((campground)=> { %>
                        <li class="p-4 bg-gray-100 rounded-lg shadow-md hover:bg-gray-200">
                            <a href="/campgrounds/<%= campground._id %>"
                                class="block w-full h-full p-4 text-blue-600 hover:underline">
                                <%= campground.title %>
                            </a>
                        </li>
                        <% }) %>
                </ul>
            </div>
    </body>
    <%- include('partials/footer.ejs') %> -->


<%- include('partials/header.ejs', { title: "Home page" }) %>

    <body class="bg-gray-50 text-gray-900 min-h-screen">
        <%- include('partials/navbar.ejs') %>

            <div class="max-w-4xl mx-auto mt-10 p-6 bg-white shadow-xl rounded-xl">
                <h2 class="text-3xl font-bold text-gray-800 mb-6">Campgrounds</h2>
                <ul class="space-y-6">
                    <% campgrounds.forEach((campground)=> { %>
                        <li class="bg-gray-100 rounded-lg shadow-md hover:shadow-lg transition overflow-hidden">
                            <a href="/campgrounds/<%= campground._id %>" class="flex flex-col sm:flex-row">
                                <img src="<%= campground.image %>" alt="<%= campground.title %>"
                                    class="w-full sm:w-48 h-48 object-cover sm:rounded-l-lg">

                                <div class="p-4 flex flex-col justify-between">
                                    <div>
                                        <h3 class="text-xl font-semibold text-blue-700 hover:underline mb-1">
                                            <%= campground.title %>
                                        </h3>
                                        <p class="text-gray-600 text-sm mb-2">
                                            📍 <%= campground.location %>
                                        </p>
                                    </div>
                                    <p class="text-gray-500 text-xs">Click for more details</p>
                                </div>
                            </a>
                        </li>
                        <% }) %>
                </ul>
            </div>

            <%- include('partials/footer.ejs') %>
    </body>